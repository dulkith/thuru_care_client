import 'package:flutter/material.dart';

class FifthScreenState extends StatelessWidget {
 final Color color;

 FifthScreenState(this.color);

 @override
  Widget build(BuildContext ctxt) {
    return new Scaffold(
      
      body: new Text("I belongs to Fifth Page"),
    );
  }
}

