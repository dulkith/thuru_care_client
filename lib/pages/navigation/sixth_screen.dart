import 'package:flutter/material.dart';

class SixthScreenState extends StatelessWidget {
 final Color color;

 SixthScreenState(this.color);

 @override
  Widget build(BuildContext ctxt) {
    return new Scaffold(
      
      body: new Text("I belongs to Sixth Page"),
    );
  }
}

