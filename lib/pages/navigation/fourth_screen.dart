import 'package:flutter/material.dart';

class FourthScreenState extends StatelessWidget {
 final Color color;

 FourthScreenState(this.color);

 @override
  Widget build(BuildContext ctxt) {
    return new Scaffold(
      
      body: new Text("I belongs to Fourth Page"),
    );
  }
}

